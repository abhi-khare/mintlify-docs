openapi: 3.0.3
info:
  title: Cosmos V2 API
  version: 1.0.0
  description: API endpoints autogenerated from Insomnia collection.
servers:
  - url: https://platform.voicelabs.in
paths:
  /api/v1/get-artifacts:
    post:
      summary: artifact
      parameters:
        - name: call_id
          in: query
          required: true
          schema:
            type: string
          description: ID of the call to retrieve artifacts for
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
        '401':
          description: Unauthorized – invalid or missing token
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
